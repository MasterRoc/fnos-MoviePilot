[
  {
    "stepTitle": "服务配置",
    "items": [
      {
        "type": "text",
        "field": "wizard_port",
        "label": "后端 API 端口",
        "rules": [
          { "pattern": "^[0-9]+$", "message": "请输入数字端口" }
        ]
      },
      {
        "type": "text",
        "field": "wizard_nginx_port",
        "label": "前端端口",
        "rules": [
          { "pattern": "^[0-9]+$", "message": "请输入数字端口" }
        ]
      }
    ]
  },
  {
    "stepTitle": "管理员密码",
    "items": [
      {
        "type": "password",
        "field": "wizard_superuser_password",
        "label": "重置超级管理员密码（留空则不修改；仅限字母、数字及 !@%^*_.:+? ，至少含其中两类）",
        "rules": [
          { "max": 50, "message": "密码长度不能超过 50 位" },
          { "pattern": "^(?=[A-Za-z0-9!@%^*_.:+?-]{1,50}$)(?:(?=.*[A-Za-z])(?=.*[0-9])|(?=.*[A-Za-z])(?=.*[!@%^*_.:+?-])|(?=.*[0-9])(?=.*[!@%^*_.:+?-])).+$", "message": "密码仅限字母、数字及 !@%^*_.:+? ，需至少包含其中两类" }
        ]
      }
    ]
  }
]
